<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.etc.blogtest.dao.TbOrderDao">

    <resultMap type="com.etc.blogtest.entity.TbOrder" id="TbOrderMap">
        <result property="payId" column="pay_id" jdbcType="VARCHAR"/>
        <result property="uid" column="uid" jdbcType="INTEGER"/>
        <result property="type" column="type" jdbcType="INTEGER"/>
        <result property="price" column="price" jdbcType="VARCHAR"/>
        <result property="reallyPrice" column="really_price" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="TbOrderMap">
        select
          pay_id, uid, type, price, really_price, create_time
        from tb_order
        where pay_id = #{payId}
    </select>



    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from tb_order
        <where>
            <if test="payId != null and payId != ''">
                and pay_id = #{payId}
            </if>
            <if test="uid != null">
                and uid = #{uid}
            </if>
            <if test="type != null">
                and type = #{type}
            </if>
            <if test="price != null and price != ''">
                and price = #{price}
            </if>
            <if test="reallyPrice != null and reallyPrice != ''">
                and really_price = #{reallyPrice}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="payId" useGeneratedKeys="true">
        insert into tb_order(uid, type, price, really_price, create_time)
        values (#{uid}, #{type}, #{price}, #{reallyPrice}, #{createTime})
    </insert>

    <insert id="insertBatch" keyProperty="payId" useGeneratedKeys="true">
        insert into tb_order(uid, type, price, really_price, create_time)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.uid}, #{entity.type}, #{entity.price}, #{entity.reallyPrice}, #{entity.createTime})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="payId" useGeneratedKeys="true">
        insert into tb_order(uid, type, price, really_price, create_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.uid}, #{entity.type}, #{entity.price}, #{entity.reallyPrice}, #{entity.createTime})
        </foreach>
        on duplicate key update
        uid = values(uid),
        type = values(type),
        price = values(price),
        really_price = values(really_price),
        create_time = values(create_time)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update tb_order
        <set>
            <if test="uid != null">
                uid = #{uid},
            </if>
            <if test="type != null">
                type = #{type},
            </if>
            <if test="price != null and price != ''">
                price = #{price},
            </if>
            <if test="reallyPrice != null and reallyPrice != ''">
                really_price = #{reallyPrice},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
        </set>
        where pay_id = #{payId}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from tb_order where pay_id = #{payId}
    </delete>

</mapper>

