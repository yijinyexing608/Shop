<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.etc.blogtest.dao.FriendsDao">

    <resultMap type="com.etc.blogtest.entity.Friends" id="FriendsMap">
        <result property="uid" column="uid" jdbcType="INTEGER"/>
        <result property="friendId" column="friend_id" jdbcType="INTEGER"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="FriendsMap">
        select
          uid, friend_id, create_time
        from friends
        where uid = #{uid}
    </select>



    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from friends
        <where>
            <if test="uid != null">
                and uid = #{uid}
            </if>
            <if test="friendId != null">
                and friend_id = #{friendId}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="uid" useGeneratedKeys="true">
        insert into friends(create_time)
        values (#{createTime})
    </insert>

    <insert id="insertBatch" keyProperty="uid" useGeneratedKeys="true">
        insert into friends(create_time)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.createTime})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="uid" useGeneratedKeys="true">
        insert into friends(create_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.createTime})
        </foreach>
        on duplicate key update
        create_time = values(create_time)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update friends
        <set>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
        </set>
        where uid = #{uid}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from friends where uid = #{uid}
    </delete>

</mapper>

