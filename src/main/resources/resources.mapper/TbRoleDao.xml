<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.etc.blogtest.dao.TbRoleDao">

    <resultMap type="com.etc.blogtest.entity.TbRole" id="TbRoleMap">
        <result property="rid" column="rid" jdbcType="INTEGER"/>
        <result property="roleName" column="role_name" jdbcType="VARCHAR"/>
        <result property="roleLabel" column="role_label" jdbcType="VARCHAR"/>
        <result property="isDisable" column="is_disable" jdbcType="INTEGER"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="TbRoleMap">
        select
          rid, role_name, role_label, is_disable, create_time, update_time
        from tb_role
        where rid = #{rid}
    </select>



    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from tb_role
        <where>
            <if test="rid != null">
                and rid = #{rid}
            </if>
            <if test="roleName != null and roleName != ''">
                and role_name = #{roleName}
            </if>
            <if test="roleLabel != null and roleLabel != ''">
                and role_label = #{roleLabel}
            </if>
            <if test="isDisable != null">
                and is_disable = #{isDisable}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="updateTime != null">
                and update_time = #{updateTime}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="rid" useGeneratedKeys="true">
        insert into tb_role(role_name, role_label, is_disable, create_time, update_time)
        values (#{roleName}, #{roleLabel}, #{isDisable}, #{createTime}, #{updateTime})
    </insert>

    <insert id="insertBatch" keyProperty="rid" useGeneratedKeys="true">
        insert into tb_role(role_name, role_label, is_disable, create_time, update_time)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.roleName}, #{entity.roleLabel}, #{entity.isDisable}, #{entity.createTime}, #{entity.updateTime})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="rid" useGeneratedKeys="true">
        insert into tb_role(role_name, role_label, is_disable, create_time, update_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.roleName}, #{entity.roleLabel}, #{entity.isDisable}, #{entity.createTime}, #{entity.updateTime})
        </foreach>
        on duplicate key update
        role_name = values(role_name),
        role_label = values(role_label),
        is_disable = values(is_disable),
        create_time = values(create_time),
        update_time = values(update_time)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update tb_role
        <set>
            <if test="roleName != null and roleName != ''">
                role_name = #{roleName},
            </if>
            <if test="roleLabel != null and roleLabel != ''">
                role_label = #{roleLabel},
            </if>
            <if test="isDisable != null">
                is_disable = #{isDisable},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
        </set>
        where rid = #{rid}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from tb_role where rid = #{rid}
    </delete>

</mapper>

