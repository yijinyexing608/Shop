<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.etc.blogtest.dao.GroupChatDao">

    <resultMap type="com.etc.blogtest.entity.GroupChat" id="GroupChatMap">
        <result property="msgId" column="msg_id" jdbcType="INTEGER"/>
        <result property="uid" column="uid" jdbcType="INTEGER"/>
        <result property="content" column="content" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="roomId" column="room_id" jdbcType="INTEGER"/>
        <result property="textType" column="text_type" jdbcType="INTEGER"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="GroupChatMap">
        select
          msg_id, uid, content, create_time, room_id, text_type
        from group_chat
        where msg_id = #{msgId}
    </select>



    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from group_chat
        <where>
            <if test="msgId != null">
                and msg_id = #{msgId}
            </if>
            <if test="uid != null">
                and uid = #{uid}
            </if>
            <if test="content != null and content != ''">
                and content = #{content}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="roomId != null">
                and room_id = #{roomId}
            </if>
            <if test="textType != null">
                and text_type = #{textType}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="msgId" useGeneratedKeys="true">
        insert into group_chat(uid, content, create_time, room_id, text_type)
        values (#{uid}, #{content}, #{createTime}, #{roomId}, #{textType})
    </insert>

    <insert id="insertBatch" keyProperty="msgId" useGeneratedKeys="true">
        insert into group_chat(uid, content, create_time, room_id, text_type)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.uid}, #{entity.content}, #{entity.createTime}, #{entity.roomId}, #{entity.textType})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="msgId" useGeneratedKeys="true">
        insert into group_chat(uid, content, create_time, room_id, text_type)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.uid}, #{entity.content}, #{entity.createTime}, #{entity.roomId}, #{entity.textType})
        </foreach>
        on duplicate key update
        uid = values(uid),
        content = values(content),
        create_time = values(create_time),
        room_id = values(room_id),
        text_type = values(text_type)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update group_chat
        <set>
            <if test="uid != null">
                uid = #{uid},
            </if>
            <if test="content != null and content != ''">
                content = #{content},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="roomId != null">
                room_id = #{roomId},
            </if>
            <if test="textType != null">
                text_type = #{textType},
            </if>
        </set>
        where msg_id = #{msgId}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from group_chat where msg_id = #{msgId}
    </delete>

</mapper>

